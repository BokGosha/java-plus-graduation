server:
  port: ${SERVER_PORT:0}

management:
  server:
    port: ${MANAGEMENT_SERVER_PORT:8085}

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5438/compilations}
